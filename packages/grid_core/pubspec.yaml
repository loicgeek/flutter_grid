name: grid_core
description: >-
  Headless, pure-Dart core for flutter_grid. Provides the controller, state
  machine, column definitions, pipeline (sort/filter/paginate/group), and the
  GridDataSource abstraction — no Flutter dependency.
version: 0.1.0
repository: https://github.com/loicgeek/flutter_grid/tree/main/packages/grid_core
issue_tracker: https://github.com/loicgeek/flutter_grid/issues
homepage: https://github.com/loicgeek/flutter_grid

topics:
  - table
  - grid
  - data-table
  - flutter-grid

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies: {}

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
